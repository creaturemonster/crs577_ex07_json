<?xml version="1.0" encoding="UTF-8" ?>
<web-app 
    xmlns="http://java.sun.com/xml/ns/javaee"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://java.sun.com/xml/ns/javaee 
                        http://java.sun.com/xml/ns/javaee/web-app_3_0.xsd"
    version="3.0">
    
    <servlet>
        <!-- TODO: Use the fully-qualified name of the JAX-RS Application subclass
                   as the value of the <servlet-name> element 
             HINT: See slide 6-12 -->
        <servlet-name>com.rf.inventory.webapps.InventoryApplication</servlet-name>
    </servlet>
    
    <servlet-mapping>
        <!-- TODO: Use the fully-qualified name of the JAX-RS Application subclass
                   as the value of the <servlet-name> element (same as above) -->
        <servlet-name>com.rf.inventory.webapps.InventoryApplication</servlet-name>

        <!-- TODO: Note the URL pattern for REST requests 
                   (no changes required) -->
        <url-pattern>/rs/*</url-pattern>
    </servlet-mapping>
    
</web-app>
